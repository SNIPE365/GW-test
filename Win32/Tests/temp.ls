//3024 P1 s1 = 18654 LA1 #2 c1; //P1 s1 is too far in, collision error with LA1 c1
//3024 P1 s1 = 18654 LA1 #2 c2; //P1 s1 is too far in, collision error with LA1 c2
                           //P1 is not being flipped 180 degrees since the s1 is still facing the same way as if we did P1 s1 = LA1 c1;
//18654 LA1 c1 = 3024 P1 #2 s1; //LA1 c1 is too far in, collision error with P1 s1
18654 LA1 c2 = 3024 P1 #2 s1; //LA1 c2 is too far in, collision error with P1 s1
		           //LA1 is not being flipped 180 degrees since c1 is still facing the same with as if we did LA1 c1 = P1 s1;